import { CustomValidator, Meta } from 'express-validator/src/base';
import { Context } from 'express-validator/src/context';
import { ContextItem } from 'express-validator/src/context-items/context-item';
export declare class CustomValidation implements ContextItem {
    private readonly validator;
    private readonly negated;
    message: any;
    constructor(validator: CustomValidator, negated: boolean);
    run(context: Context, value: any, meta: Meta): Promise<void>;
}
